# Funtion array_flip

## Description
```
Returns the values from a single column of the array, identified by the column_key. Optionally, an index_key may be
provided to index the values in the returned array by the values from the index_key column of the input array.

array_column(array $array, in|string|null $column_key, int|string|null $index_key): array
```